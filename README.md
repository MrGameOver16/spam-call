# spam-call
# install

  $ apt update && apt upgrade
  $ apt install git
  $ apt install php
  $ git clone https://github.com/MrGameOver16/spam-call.git
  $ cd spam-call
  $ php run.php

# by aadityaa
# Mr.GameOver
